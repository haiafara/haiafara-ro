# Read about fixtures at http://api.rubyonrails.org/classes/ActiveRecord/FixtureSet.html

one:
  region: one
  name: Zone One
  slug: zone-one
  default: false

two:
  region: two
  name: Zone Two
  slug: zone-two
  default: true

three:
  region: three
  name: Zone Three
  slug: zone-three
  default: false
